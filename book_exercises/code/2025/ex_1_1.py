# Exercise 1: typing an expression
# Hint: lines which have a # in front are commented out. They are ignored by Python.

# Define variable x
x = 3
# Calculate a formula and put the result in variable y
y = x**3 + 5 - (3 + x) / 2

# Print the resulting value in variable y
print(y)
# Print the type of the variable y
print(type(y))
