# Practical 6: exercise 1
import numpy as np

# Convert the list: `[1, 2, 3]` to an array using the `np.array()` method.
a = np.array([1, 2, 3])
print(a)

# Create an 3x2 array filled with zeros using `np.zeros(n, m)`, print the array.
b = np.zeros(shape=(3, 2))
print(b)

# Create an 1x5 array filled with ones using `np.ones(n, m)`, print the array.
c = np.ones(shape=(1, 5))
print(c)
